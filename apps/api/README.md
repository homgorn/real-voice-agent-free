# API App

Минимальный backend для `VoiceAgent OSS Core`.

На текущем этапе здесь живут:

- FastAPI skeleton
- app factory
- API key auth
- базовый `agents` CRUD/publish bootstrap

Хранилище пока in-memory.
Это осознанный bootstrap для Sprint 01, а не финальная persistence-модель.
